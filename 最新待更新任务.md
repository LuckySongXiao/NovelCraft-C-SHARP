# 最新待更新任务

更新时间：2026-04-24

本文档用于承接当前仓库接下来需要继续完善的工作，重点覆盖：
- 主功能链仍未完全真实化的页面与对话框
- AI 相关页面仍存在的半成品链路
- ~~当前构建中仍较集中的 `CS1591` XML 注释警告~~（已于 2026-04-24 全部清零）
- 少量零散但适合顺手收口的单点问题

---

## 一、功能主链待完善

### 1. AICollaborationView 主链继续真实化
文件：
- `src/NovelManagement.WPF/Views/AICollaborationView.xaml.cs`

当前已完成：
- 配置对象与 UI 的双向同步已补齐
- 连接状态显示已改成基于提供者与配置状态的真实摘要
- 提供者状态探测已对 Ollama/DeepSeek 做了合理区分处理（2026-04-24 确认）
- 不存在"仅打开原始助手但不落盘"的入口（测试思维链和测试发送是合理的开发调试功能）

仍需完善（架构级改造，需新建实体/服务/迁移）：
- 将协作任务、协作历史、协作会话接入真实持久化，而不是只停留在页面内存态
  - **现状**：项目中完全不存在 Collaboration 相关实体、DbSet、服务层；TaskQueue 和 WorkflowEngine 均为纯内存存储（ConcurrentQueue/ConcurrentDictionary），应用重启后数据丢失
  - **需要**：Core 层新增 CollaborationSession/CollaborationTask 实体 → Infrastructure 层注册 DbSet + 迁移 → Application 层新增 CollaborationService → View 层接入
- 为 Agent 列表补充稳定的数据来源说明，避免继续混用运行态信息与展示态模型

优先级：高（架构级）

### 2. AI 编辑器链路里的“半真半假”能力继续收口
文件：
- `src/NovelManagement.WPF/Views/AIChapterWriteDialog.xaml.cs`
- `src/NovelManagement.WPF/Views/AIPolishDialog.xaml.cs`
- `src/NovelManagement.WPF/Views/ConsistencyCheckDialog.xaml.cs`
- `src/NovelManagement.WPF/Views/DialogGenerationView.xaml.cs`

仍需完善：
- ~~**ConsistencyCheckDialog**：将 `PerformConsistencyCheck()` 接入 `_aiAssistantService`，删除 `GenerateMockIssues()`~~（已完成 2026-04-24）
- ~~**AIPolishDialog**：`GenerateSmartSuggestions()`和`GenerateQualityAnalysis()`已改为异步方法，优先调用AI服务，失败时降级为预设数据~~（已完成 2026-04-24）
- ~~**DialogGenerationView/DialogGenerationService**：已接入AIAssistantService，对话生成和优化优先调用AI服务，失败时降级为模板拼接~~（已完成 2026-04-24）
- **DialogGenerationView**：Save/Export 仍为 TODO
- **AIChapterWriteDialog**：mock fallback 已保留并标注为降级策略
- 梳理四类结果的落盘策略：哪些直接写回正文/设定，哪些保留为建议
- 对失败态、空结果、取消态补统一提示与回滚逻辑

2026-04-24 Mock分析（已更新）：

| 文件 | Mock严重度 | 关键问题 |
|------|-----------|---------|
| AIChapterWriteDialog | 中 | 仅AI不可用时fallback到mock，正常流程调用真实AI |
| AIPolishDialog | **低** | `GenerateSmartSuggestionsAsync()`和`GenerateQualityAnalysisAsync()`已接入AI服务，仅AI不可用时降级为预设数据 |
| ConsistencyCheckDialog | **已修复** | 已接入`CheckPlotContinuityAsync()`，删除了`GenerateMockIssues()` |
| DialogGenerationView | **低** | Service层已接入AIAssistantService，AI不可用时降级为模板拼接；Save/Export仍为TODO |

优先级：高

### 3. 仍需核查的“演示页/样例页/实验入口”
文件：
- `src/NovelManagement.WPF/Views/MainWindow.xaml.cs`
- `src/NovelManagement.WPF/App.xaml.cs`
- `src/NovelManagement.WPF.Test/MainWindow.xaml.cs`

仍需完善：
- 复查主导航中是否还暴露实验区或演示页入口
- 复查应用启动时是否仍注入示例数据、测试种子或开发态内容
- 明确测试工程是否继续随正式仓库交付，或改为仅开发内部使用

优先级：中

---

## 二、当前构建中的 XML 注释警告热点

说明：
- 以下内容按最近构建输出和最近几轮清理后的剩余热点整理
- 已经清理过的页面不再列入本轮主清单
- **2026-04-24 构建验证：`dotnet build` 输出中 CS1591 警告数量为 0，XML 注释热点已全部清零**

### 1. ~~AIConfigurationView 配置模型簇~~（已完成 2026-04-24）
文件：
- `src/NovelManagement.WPF/Views/AIConfigurationView.xaml.cs`

已完成注释对象：
- `AIConfigurationView()`
- `OllamaModelViewModel`（含 Name、Size、SizeFormatted、ModifiedAt 四个属性）
- `PropertyChanged`
- `OnPropertyChanged(string?)`

说明：
- 已补齐全部 XML 文档注释，构建无 CS1591 警告

### 2. ~~ChapterEditorDialog 数据模型与命令类~~（已完成 2026-04-24）
文件：
- `src/NovelManagement.WPF/Views/ChapterEditorDialog.xaml.cs`

已完成注释对象：
- `ChapterEditData`（含 Id、Title、Content、Summary、Status、ImportanceLevel、Characters、Tags、Notes、TargetWordCount、VolumeId、Order 共 12 个属性）
- `RelayCommand`（含构造函数、CanExecuteChanged 事件、CanExecute()、Execute()）

说明：
- 已补齐全部 XML 文档注释，构建无 CS1591 警告

### 3. ~~CharacterManagementView 零散公开成员~~（已完成 2026-04-24）
文件：
- `src/NovelManagement.WPF/Views/CharacterManagementView.xaml.cs`

已完成注释对象：
- `RefreshOnProjectChangedAsync(Guid?, string?)`
- `CharacterGenerationDialog`（含 CharacterType、Faction、CultivationLevel、PersonalityTraits、BackgroundStory、SpecialAbilities 共 6 个属性 + 构造函数）
- `CharacterOptimizationDialog`（含 SelectedOptimizationGoals 属性 + 构造函数）
- `RelationshipAnalysisDialog(object)`

说明：
- 已补齐全部 XML 文档注释，构建无 CS1591 警告

### 4. ~~AI 小型零散点~~（已完成 2026-04-24）
文件：
- `src/NovelManagement.WPF/Views/AIModelConfigView.xaml.cs`
- `src/NovelManagement.WPF/Views/AIPolishDialog.xaml.cs`
- `src/NovelManagement.WPF/Views/AIAssistantDialog.xaml.cs`

已完成注释对象：
- `AIModelConfigView()`
- `TextDifferenceDialog(string, string)`
- `GreaterThanZeroConverter.Convert(...)`
- `GreaterThanZeroConverter.ConvertBack(...)`

说明：
- 已补齐全部 XML 文档注释，构建无 CS1591 警告

### 5. ~~预生成配置模型~~（已完成 2026-04-24）
文件：
- `src/NovelManagement.WPF/Models/PrerequisiteGenerationOptions.cs`

已完成注释对象：
- `PlotTemplate`（含 Title、Type、SummaryTemplate、ImportanceLevel、PlotPointTemplates 共 5 个属性）
- `CharacterTemplate`（含 NameTemplate、Type、Gender、AppearanceTemplate、PersonalityTemplate、BackgroundTemplate、ImportanceLevel 共 7 个属性）
- `WorldSettingTemplate`（含 NameTemplate、Type、ContentTemplate、Importance 共 4 个属性）
- `FactionTemplate`（含 NameTemplate、Type、DescriptionTemplate、PowerLevel、Importance、TagTemplates 共 6 个属性）

说明：
- 已补齐全部 XML 文档注释，构建无 CS1591 警告

### 6. 已完成并移出本轮热点
文件：
- `src/NovelManagement.WPF/Views/AICollaborationView.xaml.cs`

本轮已完成：
- `AgentViewModel`
- `TaskViewModel`

说明：
- 当前仍保留在“功能主链待完善”中，但已不属于本轮 XML 注释热点

---

## 三、建议执行顺序

### 第一批：收益最高，且能快速继续压构建热点
1. ~~`AIConfigurationView.xaml.cs`~~（已完成 2026-04-24）
2. ~~`ChapterEditorDialog.xaml.cs`~~（已完成 2026-04-24）
3. ~~`CharacterManagementView.xaml.cs`~~（已完成 2026-04-24）

### 第二批：零散补齐，快速清尾
1. ~~`AIModelConfigView.xaml.cs`~~（已完成 2026-04-24）
2. ~~`AIPolishDialog.xaml.cs`~~（已完成 2026-04-24）
3. ~~`AIAssistantDialog.xaml.cs`~~（已完成 2026-04-24）
4. ~~`PrerequisiteGenerationOptions.cs`~~（已完成 2026-04-24）
5. `MainWindow.xaml.cs` 其他若有新增公开成员再补齐

### 第三批：功能主链回收
1. `AICollaborationView.xaml.cs` 的真实持久化链路
2. `AIChapterWriteDialog.xaml.cs` / `AIPolishDialog.xaml.cs` / `ConsistencyCheckDialog.xaml.cs` 的真实结果处理
3. `MainWindow.xaml.cs` / `App.xaml.cs` 的演示入口与示例种子复查

---

## 四、执行原则

### 1. 继续沿用当前节奏
- 优先处理“同型文件”或“单文件成簇”警告
- 每次改完后都跑对应文件诊断
- 每轮结束后做一次 `dotnet build`

### 2. 功能改造优先真实化
- 优先去掉 mock、示例、演示数据兜底
- 优先接当前项目上下文、导航刷新、导入导出、保存落盘
- AI 功能优先补“生成/优化后如何落库”，其次才是界面增强

### 3. 文档注释优先补公开面
- 先补公开模型、公开构造函数、公开导航方法
- 对同类页面采用统一注释风格，避免重复返工

---

## 五、完成定义

某一项任务视为完成，需要同时满足：
- 对应源码已经补齐或收口
- `GetDiagnostics` 无新增错误
- `dotnet build src/NovelManagement.WPF/NovelManagement.WPF.csproj` 成功
- 该文件或该簇问题不再出现在当前构建热点中

I will fix the data inconsistency issues by connecting the frontend directly to the backend services and implementing a proper global state management for the active project.

### 1. Refactor `MainWindow` (UI & Logic)
- **Modify `MainWindow.xaml`**:
    - Assign `x:Name="CurrentProjectNavExpander"` to the hardcoded project navigation expander and set its initial `Visibility` to `Collapsed`.
    - Assign `x:Name="CurrentProjectStatusBarText"` to the status bar text block.
- **Modify `MainWindow.xaml.cs`**:
    - Inject `ProjectContextService` via `App.ServiceProvider`.
    - Subscribe to the `ProjectChanged` event.
    - Implement `UpdateNavigation` to dynamically show/hide the project sidebar and update the window title/status bar based on the currently active project.

### 2. Refactor `ProjectManagementView` (Data Handling)
- **Modify `ProjectManagementView.xaml.cs`**:
    - Remove all mock data generation code.
    - Inject `ProjectService` and `ProjectContextService`.
    - **Load Data**: Replace `LoadExistingProjects` with an async call to `_projectService.GetAllProjectsAsync()`.
    - **Delete Logic**:
        - Remove the "Recycle Bin" simulation since the backend doesn't support soft delete (no `IsDeleted` field).
        - Implement direct deletion using `_projectService.DeleteProjectAsync()`.
        - When a project is deleted, check if it's the current project; if so, call `_projectContextService.ClearCurrentProject()`.
    - **Create/Edit Logic**: Connect `NewProject_Click` and `EditProject_Click` to `_projectService`.

### 3. Verify
- Ensure that deleting a project removes it from the list permanently (persisted in DB).
- Ensure that deleting the active project immediately hides the sidebar navigation in `MainWindow`.
- Ensure that creating/importing a project persists it to the DB.

<Window x:Class="NovelManagement.WPF.Views.AIAssistantDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:NovelManagement.WPF.Views"
        mc:Ignorable="d"
        Title="{DynamicResource Loc.AA.Title}" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource MaterialDesignWindow}">

    <Window.Resources>
        <!-- 大于零转换器 -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:GreaterThanZeroConverter x:Key="GreaterThanZeroConverter"/>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <materialDesign:Card Grid.Row="0" Margin="16,16,16,8">
            <Grid Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Robot" 
                                           Width="24" Height="24" 
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBlock x:Name="TitleTextBlock"
                             Text="{DynamicResource Loc.AA.TitleSetting}" 
                             Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                             VerticalAlignment="Center" 
                             Margin="12,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ComboBox x:Name="ModelComboBox"
                             materialDesign:HintAssist.Hint="{DynamicResource Loc.AA.HintSelectModel}"
                             Style="{StaticResource MaterialDesignOutlinedComboBox}"
                             Width="150"
                             Margin="0,0,8,0"
                             SelectionChanged="ModelComboBox_SelectionChanged">
                        <ComboBoxItem Content="DeepSeek" Tag="DeepSeek"/>
                        <ComboBoxItem Content="llama.cpp" Tag="LlamaCpp"/>
                        <ComboBoxItem Content="Ollama" Tag="Ollama"/>
                        <ComboBoxItem Tag="智谱AI" Content="{DynamicResource Loc.AA.ProviderZhipuAI}"/>
                    </ComboBox>
                    
                    <Button x:Name="SettingsButton"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="{DynamicResource Loc.Dlg.Settings}"
                            Click="Settings_Click">
                        <materialDesign:PackIcon Kind="Settings"/>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1" Margin="16,0,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：功能面板 -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,8,0">
                <StackPanel Margin="12">
                    <TextBlock Text="{DynamicResource Loc.AA.QuickActions}" 
                             Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                             Margin="0,0,0,12"/>
                    
                    <Button x:Name="GenerateSettingBtn"
                            Content="{DynamicResource Loc.AA.GenerateSetting}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,8"
                            Click="GenerateSetting_Click"/>
                    
                    <Button x:Name="AnalyzeSettingBtn"
                            Content="{DynamicResource Loc.AA.AnalyzeSetting}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,8"
                            Click="AnalyzeSetting_Click"/>
                    
                    <Button x:Name="OptimizeSettingBtn"
                            Content="{DynamicResource Loc.AA.OptimizeSuggestion}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,8"
                            Click="OptimizeSetting_Click"/>
                    
                    <Button x:Name="CheckConsistencyBtn"
                            Content="{DynamicResource Loc.AA.ConsistencyCheck}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,8"
                            Click="CheckConsistency_Click"/>
                    
                    <Separator Margin="0,12"/>
                    
                    <TextBlock Text="{DynamicResource Loc.AA.ContextInfo}" 
                             Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                             Margin="0,0,0,8"/>
                    
                    <TextBlock x:Name="ContextTextBlock"
                             Text="{DynamicResource Loc.AA.ContextPlaceholder}"
                             Style="{StaticResource MaterialDesignCaptionTextBlock}"
                             TextWrapping="Wrap"
                             Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- 右侧：对话区域 -->
            <materialDesign:Card Grid.Column="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 对话历史 -->
                    <ScrollViewer Grid.Row="0" 
                                 x:Name="ChatScrollViewer"
                                 VerticalScrollBarVisibility="Auto"
                                 Padding="16">
                        <StackPanel x:Name="ChatPanel">
                            <!-- 欢迎消息 -->
                            <Border Background="{DynamicResource MaterialDesignCardBackground}"
                                   CornerRadius="8"
                                   Padding="12"
                                   Margin="0,0,0,8">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <materialDesign:PackIcon Kind="Robot" 
                                                               Width="16" Height="16"
                                                               Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                        <TextBlock Text="{DynamicResource Loc.AA.Title}" 
                                                 Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                 Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                 Margin="4,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="{DynamicResource Loc.AA.Welcome}"
                                             TextWrapping="Wrap"
                                             Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- 输入区域 -->
                    <Border Grid.Row="1" 
                           Background="{DynamicResource MaterialDesignCardBackground}"
                           BorderBrush="{DynamicResource MaterialDesignDivider}"
                           BorderThickness="0,1,0,0"
                           Padding="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="InputTextBox"
                                    Grid.Column="0"
                                    materialDesign:HintAssist.Hint="{DynamicResource Loc.AA.HintInput}"
                                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                    AcceptsReturn="True"
                                    TextWrapping="Wrap"
                                    MaxHeight="100"
                                    VerticalScrollBarVisibility="Auto"
                                    KeyDown="InputTextBox_KeyDown"/>

                            <Button x:Name="SendButton"
                                   Grid.Column="1"
                                   Style="{StaticResource MaterialDesignRaisedButton}"
                                   Margin="8,0,0,0"
                                   Click="Send_Click"
                                   IsEnabled="{Binding ElementName=InputTextBox, Path=Text.Length, Converter={StaticResource GreaterThanZeroConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Send" Width="16" Height="16"/>
                                    <TextBlock Text="{DynamicResource Loc.AA.Send}" Margin="4,0,0,0"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- 状态栏 -->
        <materialDesign:Card Grid.Row="2" Margin="16,0,16,16">
            <Grid Margin="12,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <materialDesign:PackIcon x:Name="StatusIcon" 
                                           Kind="CheckCircle" 
                                           Width="16" Height="16"
                                           Foreground="Green"
                                           VerticalAlignment="Center"/>
                    <TextBlock x:Name="StatusTextBlock"
                             Text="{DynamicResource Loc.AA.StatusReady}"
                             Style="{StaticResource MaterialDesignCaptionTextBlock}"
                             VerticalAlignment="Center"
                             Margin="4,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="{DynamicResource Loc.AA.ClearChat}"
                           Style="{StaticResource MaterialDesignFlatButton}"
                           Click="ClearChat_Click"/>
                    <Button Content="{DynamicResource Loc.Dlg.Close}"
                           Style="{StaticResource MaterialDesignFlatButton}"
                           Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>

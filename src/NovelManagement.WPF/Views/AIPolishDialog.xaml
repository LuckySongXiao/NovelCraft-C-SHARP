<Window x:Class="NovelManagement.WPF.Views.AIPolishDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{DynamicResource Loc.POL.Title}" Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题区域 -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,16" Padding="16">
            <StackPanel>
                <DockPanel>
                    <TextBlock Text="{DynamicResource Loc.POL.Title}"
                             FontSize="24"
                             FontWeight="Bold"
                             DockPanel.Dock="Left"/>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <!-- AI模型选择（运行时加载真实可用模型） -->
                        <ComboBox x:Name="AIModelComboBox"
                                materialDesign:HintAssist.Hint="{DynamicResource Loc.POL.HintModel}"
                                Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                Width="220"
                                Margin="0,0,8,0"/>
                        <!-- 润色历史 -->
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                              Click="ShowHistory_Click"
                              ToolTip="{DynamicResource Loc.POL.HistoryTip}">
                            <materialDesign:PackIcon Kind="History"/>
                        </Button>
                        <!-- 批量润色 -->
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                              Click="BatchPolish_Click"
                              ToolTip="{DynamicResource Loc.POL.BatchTip}"
                              Margin="4,0,0,0">
                            <materialDesign:PackIcon Kind="FormatListBulleted"/>
                        </Button>
                    </StackPanel>
                </DockPanel>

                <TextBlock Text="{DynamicResource Loc.POL.Desc}"
                         FontSize="14"
                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                         Margin="0,8,0,0"/>

                <!-- 进度指示器 -->
                <ProgressBar x:Name="PolishProgressBar"
                           Height="4"
                           Margin="0,12,0,0"
                           Visibility="Collapsed"
                           Style="{StaticResource MaterialDesignLinearProgressBar}"/>

                <!-- 状态显示 -->
                <TextBlock x:Name="PolishStatusTextBlock"
                         Text=""
                         Style="{StaticResource MaterialDesignCaptionTextBlock}"
                         Margin="0,4,0,0"
                         Visibility="Collapsed"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- 润色设置 -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="{DynamicResource Loc.POL.Settings}" 
                         FontSize="16" 
                         FontWeight="Medium" 
                         Margin="0,0,0,16"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 目标风格 -->
                    <ComboBox x:Name="TargetStyleComboBox"
                            Grid.Column="0"
                            materialDesign:HintAssist.Hint="{DynamicResource Loc.POL.HintTargetStyle}"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}">
                        <ComboBoxItem Tag="古典雅致" Content="{DynamicResource Loc.POL.StyleElegant}" IsSelected="True"/>
                        <ComboBoxItem Tag="现代简洁" Content="{DynamicResource Loc.POL.StyleConcise}"/>
                        <ComboBoxItem Tag="诗意优美" Content="{DynamicResource Loc.POL.StylePoetic}"/>
                        <ComboBoxItem Tag="生动活泼" Content="{DynamicResource Loc.POL.StyleLively}"/>
                        <ComboBoxItem Tag="庄重严肃" Content="{DynamicResource Loc.POL.StyleSolemn}"/>
                        <ComboBoxItem Tag="幽默风趣" Content="{DynamicResource Loc.POL.StyleHumorous}"/>
                    </ComboBox>

                    <!-- 润色强度 -->
                    <ComboBox x:Name="PolishIntensityComboBox"
                            Grid.Column="2"
                            materialDesign:HintAssist.Hint="{DynamicResource Loc.POL.HintIntensity}"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}">
                        <ComboBoxItem Tag="轻度润色" Content="{DynamicResource Loc.POL.IntensityLight}"/>
                        <ComboBoxItem Tag="中度润色" Content="{DynamicResource Loc.POL.IntensityMedium}" IsSelected="True"/>
                        <ComboBoxItem Tag="深度润色" Content="{DynamicResource Loc.POL.IntensityDeep}"/>
                    </ComboBox>

                    <!-- 保持要素 -->
                    <ComboBox x:Name="PreserveElementsComboBox"
                            Grid.Column="4"
                            materialDesign:HintAssist.Hint="{DynamicResource Loc.POL.HintPreserve}"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}">
                        <ComboBoxItem Tag="保持原意" Content="{DynamicResource Loc.POL.PreserveMeaning}" IsSelected="True"/>
                        <ComboBoxItem Tag="保持结构" Content="{DynamicResource Loc.POL.PreserveStructure}"/>
                        <ComboBoxItem Tag="保持风格" Content="{DynamicResource Loc.POL.PreserveStyle}"/>
                        <ComboBoxItem Tag="全面优化" Content="{DynamicResource Loc.POL.PreserveAll}"/>
                    </ComboBox>

                    <!-- 润色按钮 -->
                    <Button x:Name="PolishButton"
                          Grid.Column="6"
                          Style="{StaticResource MaterialDesignRaisedButton}"
                          Click="Polish_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AutoFix" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Loc.POL.StartPolish}"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- 高级润色选项 -->
                <Expander Header="{DynamicResource Loc.POL.AdvancedOptions}" Margin="0,16,0,0">
                    <Grid Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <!-- 润色重点 -->
                        <ComboBox x:Name="PolishFocusComboBox"
                                Grid.Row="0" Grid.Column="0"
                                materialDesign:HintAssist.Hint="{DynamicResource Loc.POL.HintFocus}"
                                Style="{StaticResource MaterialDesignOutlinedComboBox}">
                            <ComboBoxItem Tag="词汇优化" Content="{DynamicResource Loc.POL.FocusVocab}"/>
                            <ComboBoxItem Tag="句式调整" Content="{DynamicResource Loc.POL.FocusSentence}"/>
                            <ComboBoxItem Tag="语法修正" Content="{DynamicResource Loc.POL.FocusGrammar}"/>
                            <ComboBoxItem Tag="风格统一" Content="{DynamicResource Loc.POL.FocusStyle}" IsSelected="True"/>
                            <ComboBoxItem Tag="全面润色" Content="{DynamicResource Loc.POL.FocusAll}"/>
                        </ComboBox>

                        <!-- 情感基调 -->
                        <ComboBox x:Name="EmotionalToneComboBox"
                                Grid.Row="0" Grid.Column="2"
                                materialDesign:HintAssist.Hint="{DynamicResource Loc.POL.HintEmotionalTone}"
                                Style="{StaticResource MaterialDesignOutlinedComboBox}">
                            <ComboBoxItem Tag="保持原有" Content="{DynamicResource Loc.POL.ToneKeep}"/>
                            <ComboBoxItem Tag="温和平静" Content="{DynamicResource Loc.POL.ToneCalm}"/>
                            <ComboBoxItem Tag="激昂热烈" Content="{DynamicResource Loc.POL.TonePassionate}"/>
                            <ComboBoxItem Tag="深沉内敛" Content="{DynamicResource Loc.POL.ToneIntroverted}"/>
                            <ComboBoxItem Tag="轻松愉快" Content="{DynamicResource Loc.POL.ToneRelaxed}"/>
                        </ComboBox>

                        <!-- 目标读者 -->
                        <ComboBox x:Name="TargetAudienceComboBox"
                                Grid.Row="0" Grid.Column="4"
                                materialDesign:HintAssist.Hint="{DynamicResource Loc.POL.HintAudience}"
                                Style="{StaticResource MaterialDesignOutlinedComboBox}">
                            <ComboBoxItem Tag="通用读者" Content="{DynamicResource Loc.POL.AudienceGeneral}" IsSelected="True"/>
                            <ComboBoxItem Tag="青少年" Content="{DynamicResource Loc.POL.AudienceTeen}"/>
                            <ComboBoxItem Tag="成年读者" Content="{DynamicResource Loc.POL.AudienceAdult}"/>
                            <ComboBoxItem Tag="专业读者" Content="{DynamicResource Loc.POL.AudiencePro}"/>
                        </ComboBox>

                        <!-- 智能选项 -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" Orientation="Horizontal" Margin="0,12,0,0">
                            <CheckBox x:Name="AutoCorrectCheckBox" Content="{DynamicResource Loc.POL.AutoCorrect}" Margin="0,0,16,0"/>
                            <CheckBox x:Name="EnhanceDescriptionCheckBox" Content="{DynamicResource Loc.POL.EnhanceDescription}" Margin="0,0,16,0"/>
                            <CheckBox x:Name="OptimizeDialogueCheckBox" Content="{DynamicResource Loc.POL.OptimizeDialogue}" Margin="0,0,16,0"/>
                            <CheckBox x:Name="PreserveStyleCheckBox" Content="{DynamicResource Loc.POL.KeepOriginalStyle}" IsChecked="True"/>
                        </StackPanel>
                    </Grid>
                </Expander>

                <!-- 特殊要求 -->
                <TextBox x:Name="SpecialRequirementsTextBox"
                       materialDesign:HintAssist.Hint="{DynamicResource Loc.POL.HintSpecialReq}"
                       Style="{StaticResource MaterialDesignOutlinedTextBox}"
                       Margin="0,12,0,0"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- 对比区域 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- 原文 -->
            <materialDesign:Card Grid.Column="0" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,16">
                        <TextBlock Text="{DynamicResource Loc.POL.Original}"
                                 FontSize="16"
                                 FontWeight="Medium"
                                 DockPanel.Dock="Left"/>
                        <TextBlock x:Name="OriginalWordCountLabel"
                                 Text="{DynamicResource Loc.POL.ZeroWords}"
                                 FontSize="14"
                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                 DockPanel.Dock="Right"/>
                    </DockPanel>

                    <TextBox x:Name="OriginalContentTextBox"
                           Grid.Row="1"
                           Style="{StaticResource MaterialDesignOutlinedTextBox}"
                           TextWrapping="Wrap"
                           AcceptsReturn="True"
                           VerticalScrollBarVisibility="Auto"
                           IsReadOnly="True"
                           Background="{DynamicResource MaterialDesignTextFieldBoxDisabledBackground}"/>
                </Grid>
            </materialDesign:Card>

            <!-- 润色结果和替换项 -->
            <materialDesign:Card Grid.Column="2" Padding="16">
                <TabControl Style="{StaticResource MaterialDesignTabControl}">
                    <!-- 润色预览 -->
                    <TabItem Header="{DynamicResource Loc.POL.TabPreview}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <DockPanel Grid.Row="0" Margin="0,0,0,16">
                                <TextBlock Text="{DynamicResource Loc.POL.Result}"
                                         FontSize="16"
                                         FontWeight="Medium"
                                         DockPanel.Dock="Left"/>
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                    <TextBlock x:Name="PolishedWordCountLabel"
                                             Text="{DynamicResource Loc.POL.ZeroWords}"
                                             FontSize="14"
                                             Foreground="{DynamicResource MaterialDesignBodyLight}"
                                             Margin="0,0,8,0"/>
                                    <TextBlock x:Name="ImprovementLabel"
                                             Text=""
                                             FontSize="12"
                                             Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                </StackPanel>
                            </DockPanel>

                            <TextBox x:Name="PolishedContentTextBox"
                                   Grid.Row="1"
                                   Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                   TextWrapping="Wrap"
                                   AcceptsReturn="True"
                                   VerticalScrollBarVisibility="Auto"
                                   IsReadOnly="False"
                                   materialDesign:HintAssist.Hint="{DynamicResource Loc.POL.HintPolishedPlaceholder}"
                                   TextChanged="PolishedContentTextBox_TextChanged"/>

                            <!-- 操作按钮 -->
                            <StackPanel Grid.Row="2"
                                      Orientation="Horizontal"
                                      HorizontalAlignment="Right"
                                      Margin="0,16,0,0">
                                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                      Margin="0,0,8,0"
                                      Click="RePolish_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Refresh" Margin="0,0,4,0"/>
                                        <TextBlock Text="{DynamicResource Loc.POL.RePolish}"/>
                                    </StackPanel>
                                </Button>

                                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                      Margin="0,0,8,0"
                                      Click="CopyToClipboard_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ContentCopy" Margin="0,0,4,0"/>
                                        <TextBlock Text="{DynamicResource Loc.Dlg.Copy}"/>
                                    </StackPanel>
                                </Button>

                                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                      Margin="0,0,8,0"
                                      Click="ShowDifferences_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Compare" Margin="0,0,4,0"/>
                                        <TextBlock Text="{DynamicResource Loc.POL.ShowDiff}"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </TabItem>

                    <!-- 替换项列表 -->
                    <TabItem Header="{DynamicResource Loc.POL.TabReplacements}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 替换项统计 -->
                            <DockPanel Grid.Row="0" Margin="0,0,0,16">
                                <TextBlock Text="{DynamicResource Loc.POL.ReplacementList}"
                                         FontSize="16"
                                         FontWeight="Medium"
                                         DockPanel.Dock="Left"/>
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                    <TextBlock x:Name="ReplacementStatsLabel"
                                             Text="{DynamicResource Loc.POL.Replacements0}"
                                             FontSize="12"
                                             Foreground="{DynamicResource MaterialDesignBodyLight}"
                                             VerticalAlignment="Center"
                                             Margin="0,0,16,0"/>
                                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                          Margin="0,0,8,0"
                                          Click="SelectAll_Click">
                                        <TextBlock Text="{DynamicResource Loc.Dlg.SelectAll}"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                          Click="DeselectAll_Click">
                                        <TextBlock Text="{DynamicResource Loc.POL.DeselectAll}"/>
                                    </Button>
                                </StackPanel>
                            </DockPanel>

                            <!-- 替换项列表 -->
                            <ListView x:Name="ReplacementsListView"
                                    Grid.Row="1"
                                    Style="{StaticResource MaterialDesignListView}"
                                    SelectionChanged="ReplacementsListView_SelectionChanged">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Width="40">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                            Checked="ReplacementCheckBox_Changed"
                                                            Unchecked="ReplacementCheckBox_Changed"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>

                                        <GridViewColumn Header="{DynamicResource Loc.POL.Original}" Width="120">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding OriginalText}"
                                                             TextWrapping="Wrap"
                                                             MaxWidth="120"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>

                                        <GridViewColumn Header="{DynamicResource Loc.POL.ColReplaceWith}" Width="120">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding ReplacementText}"
                                                             TextWrapping="Wrap"
                                                             MaxWidth="120"
                                                             Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>

                                        <GridViewColumn Header="{DynamicResource Loc.POL.ColType}" Width="80">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding ReplacementType}"
                                                             FontSize="12"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>

                                        <GridViewColumn Header="{DynamicResource Loc.POL.ColContext}" Width="200">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Context}"
                                                             TextWrapping="Wrap"
                                                             MaxWidth="200"
                                                             FontSize="11"
                                                             Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>

                            <!-- 应用选择按钮 -->
                            <StackPanel Grid.Row="2"
                                      Orientation="Horizontal"
                                      HorizontalAlignment="Right"
                                      Margin="0,16,0,0">
                                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                      Click="ApplySelectedReplacements_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Check" Margin="0,0,4,0"/>
                                        <TextBlock Text="{DynamicResource Loc.POL.ApplySelected}"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </TabItem>

                    <!-- 智能建议 -->
                    <TabItem Header="{DynamicResource Loc.POL.TabSuggestions}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 建议标题 -->
                            <DockPanel Grid.Row="0" Margin="0,0,0,16">
                                <TextBlock Text="{DynamicResource Loc.POL.SuggestionsTitle}"
                                         FontSize="16"
                                         FontWeight="Medium"
                                         DockPanel.Dock="Left"/>
                                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                      DockPanel.Dock="Right"
                                      Click="RefreshSuggestions_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Refresh" Margin="0,0,4,0"/>
                                        <TextBlock Text="{DynamicResource Loc.POL.RefreshSuggestions}"/>
                                    </StackPanel>
                                </Button>
                            </DockPanel>

                            <!-- 建议列表 -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="SuggestionsPanel">
                                    <!-- 动态添加建议项 -->
                                </StackPanel>
                            </ScrollViewer>

                            <!-- 应用建议按钮 -->
                            <StackPanel Grid.Row="2"
                                      Orientation="Horizontal"
                                      HorizontalAlignment="Right"
                                      Margin="0,16,0,0">
                                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                      Click="ApplyAllSuggestions_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="CheckAll" Margin="0,0,4,0"/>
                                        <TextBlock Text="{DynamicResource Loc.POL.ApplyAllSuggestions}"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </TabItem>

                    <!-- 质量分析 -->
                    <TabItem Header="{DynamicResource Loc.POL.TabQuality}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 分析标题 -->
                            <TextBlock Grid.Row="0"
                                     Text="{DynamicResource Loc.POL.QualityTitle}"
                                     FontSize="16"
                                     FontWeight="Medium"
                                     Margin="0,0,0,16"/>

                            <!-- 分析内容 -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="QualityAnalysisPanel">
                                    <!-- 动态添加分析项 -->
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </TabItem>
                </TabControl>
            </materialDesign:Card>
        </Grid>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="3" 
                  Orientation="Horizontal" 
                  HorizontalAlignment="Right"
                  Margin="0,24,0,0">
            <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                  Margin="0,0,8,0"
                  Click="Cancel_Click">
                <TextBlock Text="{DynamicResource Loc.Dlg.Cancel}"/>
            </Button>
            
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                  Click="Apply_Click"
                  x:Name="ApplyButton">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Check" Margin="0,0,4,0"/>
                    <TextBlock Text="{DynamicResource Loc.POL.ApplyPolish}"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>

    <Window.Resources>
    </Window.Resources>
</Window>

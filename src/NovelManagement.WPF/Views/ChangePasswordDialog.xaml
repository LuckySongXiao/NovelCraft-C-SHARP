<Window x:Class="NovelManagement.WPF.Views.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Loc.CPW.Title}"
        Width="440" Height="330"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource RegionBrush}">
    <Grid Margin="28,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{DynamicResource Loc.CPW.Title}" FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
        <TextBlock Grid.Row="1" Text="{DynamicResource Loc.CPW.EffectNotice}" FontSize="12" Margin="0,6,0,14"
                   HorizontalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{DynamicResource Loc.CPW.CurrentPassword}" VerticalAlignment="Center" FontSize="13"/>
            <PasswordBox x:Name="CurrentPasswordBox" Grid.Column="1" Height="32" FontSize="14"
                         VerticalContentAlignment="Center" PasswordChar="●"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{DynamicResource Loc.CPW.NewPassword}" VerticalAlignment="Center" FontSize="13"/>
            <PasswordBox x:Name="NewPasswordBox" Grid.Column="1" Height="32" FontSize="14"
                         VerticalContentAlignment="Center" PasswordChar="●"/>
        </Grid>

        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{DynamicResource Loc.CPW.ConfirmNewPassword}" VerticalAlignment="Center" FontSize="13"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Grid.Column="1" Height="32" FontSize="14"
                         VerticalContentAlignment="Center" PasswordChar="●"/>
        </Grid>

        <TextBlock x:Name="ErrorText" Grid.Row="5" Text="" FontSize="12" Margin="0,10,0,0"
                   Foreground="#E53935" HorizontalAlignment="Center" Visibility="Collapsed"
                   TextWrapping="Wrap"/>

        <Grid Grid.Row="7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="ConfirmButton" Grid.Column="0" Content="{DynamicResource Loc.CPW.Confirm}" Height="34" FontSize="14"
                    Click="Confirm_Click"/>
            <Button x:Name="CancelButton" Grid.Column="2" Content="{DynamicResource Loc.CPW.Cancel}" Height="34" FontSize="14"
                    IsCancel="True" Click="Cancel_Click"/>
        </Grid>
    </Grid>
</Window>

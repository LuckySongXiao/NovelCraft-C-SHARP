<UserControl x:Class="NovelManagement.WPF.Views.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:localization="clr-namespace:NovelManagement.WPF.Localization"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             Background="{DynamicResource MaterialDesignPaper}">

    <UserControl.Resources>
        <localization:StoredValueConverter x:Key="StoredValueConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" 
                Background="{DynamicResource PrimaryHueMidBrush}" 
                Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Timeline" 
                                           Foreground="White" 
                                           Width="32" 
                                           Height="32" 
                                           VerticalAlignment="Center"/>
                    <TextBlock Text="{DynamicResource Loc.TL.Title}" 
                             Foreground="White" 
                             FontSize="24" 
                             FontWeight="Medium" 
                             VerticalAlignment="Center" 
                             Margin="16,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignIconForegroundButton}" 
                            Foreground="White" 
                            Margin="8,0"
                            Click="AddEvent_Click"
                            ToolTip="{DynamicResource Loc.TL.NewEvent}">
                        <materialDesign:PackIcon Kind="Plus" Width="24" Height="24"/>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignIconForegroundButton}" 
                            Foreground="White" 
                            Margin="8,0"
                            Click="ImportTimeline_Click"
                            ToolTip="{DynamicResource Loc.TL.ImportTooltip}">
                        <materialDesign:PackIcon Kind="Import" Width="24" Height="24"/>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Foreground="White"
                            Margin="8,0"
                            Click="ExportTimeline_Click"
                            ToolTip="{DynamicResource Loc.TL.ExportTooltip}">
                        <materialDesign:PackIcon Kind="Export" Width="24" Height="24"/>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Foreground="White"
                            Margin="8,0"
                            Click="AIAssistant_Click"
                            ToolTip="{DynamicResource Loc.TL.AIAssistantTooltip}">
                        <materialDesign:PackIcon Kind="Robot" Width="24" Height="24"/>
                    </Button>

                    <!-- 测试按钮 -->
                    <Button Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Foreground="White"
                            Margin="8,0"
                            Click="TestSelect_Click"
                            ToolTip="{DynamicResource Loc.TL.TestSelectionTooltip}">
                        <materialDesign:PackIcon Kind="TestTube" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：时间线事件列表 -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,12,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 搜索栏 -->
                    <Border Grid.Row="0" Padding="16,16,16,8">
                        <TextBox x:Name="SearchTextBox"
                                materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.SearchHint}"
                                materialDesign:TextFieldAssist.HasLeadingIcon="True"
                                materialDesign:TextFieldAssist.LeadingIcon="Magnify"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                TextChanged="SearchTextBox_TextChanged"/>
                    </Border>

                    <!-- 筛选器 -->
                    <Border Grid.Row="1" Padding="16,0,16,8">
                        <StackPanel>
                            <ComboBox x:Name="CategoryFilterComboBox"
                                     materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.CategoryHint}"
                                     Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                     SelectionChanged="CategoryFilter_SelectionChanged">
                                <ComboBoxItem Content="{DynamicResource Loc.TL.CategoryAll}"/>
                                <ComboBoxItem Tag="历史事件" Content="{DynamicResource Loc.TL.Data.历史事件}"/>
                                <ComboBoxItem Tag="剧情事件" Content="{DynamicResource Loc.TL.Data.剧情事件}"/>
                                <ComboBoxItem Tag="角色事件" Content="{DynamicResource Loc.TL.Data.角色事件}"/>
                                <ComboBoxItem Tag="势力事件" Content="{DynamicResource Loc.TL.Data.势力事件}"/>
                                <ComboBoxItem Tag="世界事件" Content="{DynamicResource Loc.TL.Data.世界事件}"/>
                                <ComboBoxItem Tag="修炼事件" Content="{DynamicResource Loc.TL.Data.修炼事件}"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                    <!-- 时间线事件列表 -->
                    <ListView x:Name="TimelineListControl"
                            Grid.Row="2"
                            SelectionMode="Single"
                            SelectionChanged="TimelineList_SelectionChanged"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                            ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,8"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                                <DataTemplate>
                                    <!-- 事件卡片容器 -->
                                    <materialDesign:Card Margin="0,0,0,8"
                                                       materialDesign:ShadowAssist.ShadowDepth="Depth1"
                                                       materialDesign:ElevationAssist.Elevation="Dp2"
                                                       x:Name="EventCard"
                                                       MouseLeftButtonUp="EventCard_MouseLeftButtonUp">
                                        <materialDesign:Card.Style>
                                            <Style TargetType="materialDesign:Card">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp4"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </materialDesign:Card.Style>

                                        <Grid Cursor="Hand">
                                            <!-- 事件内容 -->
                                                <!-- 左侧时间线指示器 -->
                                                <Border Width="4"
                                                      HorizontalAlignment="Left"
                                                      Background="{DynamicResource PrimaryHueMidBrush}"/>

                                                <!-- 主要内容区域 -->
                                                <StackPanel Margin="16,12,16,12">
                                                    <!-- 标题行 -->
                                                    <Grid Margin="0,0,0,8">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <!-- 事件图标 -->
                                                        <Border Grid.Column="0"
                                                              Background="{DynamicResource PrimaryHueLightBrush}"
                                                              CornerRadius="16"
                                                              Width="32"
                                                              Height="32"
                                                              VerticalAlignment="Top">
                                                            <materialDesign:PackIcon Kind="{Binding CategoryIcon}"
                                                                                   Width="18"
                                                                                   Height="18"
                                                                                   Foreground="White"
                                                                                   HorizontalAlignment="Center"
                                                                                   VerticalAlignment="Center"/>
                                                        </Border>

                                                        <!-- 标题和时间 -->
                                                        <StackPanel Grid.Column="1"
                                                                  Margin="12,0,8,0"
                                                                  VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding Title}"
                                                                     FontWeight="SemiBold"
                                                                     FontSize="15"
                                                                     Foreground="{DynamicResource MaterialDesignBody}"
                                                                     TextTrimming="CharacterEllipsis"/>
                                                            <TextBlock Text="{Binding TimeDisplay}"
                                                                     FontSize="12"
                                                                     FontWeight="Medium"
                                                                     Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                                     Margin="0,2,0,0"/>
                                                        </StackPanel>

                                                        <!-- 类别标签 -->
                                                        <Border Grid.Column="2"
                                                              Background="{DynamicResource PrimaryHueLightBrush}"
                                                              CornerRadius="12"
                                                              Padding="8,4"
                                                              VerticalAlignment="Top">
                                                            <TextBlock Text="{Binding Category, Converter={StaticResource StoredValueConverter}}"
                                                                     Foreground="White"
                                                                     FontSize="11"
                                                                     FontWeight="Medium"/>
                                                        </Border>
                                                    </Grid>

                                                    <!-- 描述文本 -->
                                                    <TextBlock Text="{Binding Description}"
                                                             Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                             FontSize="13"
                                                             TextWrapping="Wrap"
                                                             MaxHeight="40"
                                                             TextTrimming="WordEllipsis"
                                                             LineHeight="18"
                                                             Margin="44,0,0,8"/>

                                                    <!-- 底部信息栏 -->
                                                    <Grid Margin="44,0,0,0">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <!-- 左侧信息 -->
                                                        <StackPanel Grid.Column="0"
                                                                  Orientation="Horizontal"
                                                                  VerticalAlignment="Center">
                                                            <!-- 参与者信息 -->
                                                            <Border Background="{DynamicResource MaterialDesignDivider}"
                                                                  CornerRadius="8"
                                                                  Padding="6,2"
                                                                  Margin="0,0,8,0">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <materialDesign:PackIcon Kind="Account"
                                                                                           Width="12"
                                                                                           Height="12"
                                                                                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                                                           VerticalAlignment="Center"/>
                                                                    <TextBlock Text="{Binding ParticipantCount}"
                                                                             FontSize="11"
                                                                             Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                                             Margin="4,0,0,0"
                                                                             VerticalAlignment="Center"/>
                                                                </StackPanel>
                                                            </Border>

                                                            <!-- 重要程度指示器 -->
                                                            <Border CornerRadius="8"
                                                                  Padding="6,2"
                                                                  Background="{Binding ImportanceColor}">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <materialDesign:PackIcon Kind="Star"
                                                                                           Width="12"
                                                                                           Height="12"
                                                                                           Foreground="White"
                                                                                           VerticalAlignment="Center"/>
                                                                    <TextBlock Text="{Binding Importance}"
                                                                             FontSize="11"
                                                                             Foreground="White"
                                                                             FontWeight="Medium"
                                                                             Margin="4,0,0,0"
                                                                             VerticalAlignment="Center"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </StackPanel>

                                                        <!-- 右侧状态 -->
                                                        <Border Grid.Column="1"
                                                              CornerRadius="8"
                                                              Padding="6,2"
                                                              Background="{Binding StatusColor}">
                                                            <TextBlock Text="{Binding Status, Converter={StaticResource StoredValueConverter}}"
                                                                     FontSize="11"
                                                                     Foreground="White"
                                                                     FontWeight="Medium"/>
                                                        </Border>
                                                    </Grid>
                                                </StackPanel>
                                        </Grid>
                                    </materialDesign:Card>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                    <!-- 统计信息 -->
                    <Border Grid.Row="3" 
                          Background="{DynamicResource MaterialDesignDivider}" 
                          Padding="16,8">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Loc.TL.Stats}" 
                                     FontWeight="Medium" 
                                     FontSize="12" 
                                     Margin="0,0,0,4"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock FontSize="11" Foreground="{DynamicResource MaterialDesignBodyLight}">
                                        <Run Text="{DynamicResource Loc.TL.TotalLabel}"/>
                                        <Run Text="{Binding TotalCount, Mode=OneWay}" FontWeight="Medium"/>
                                    </TextBlock>
                                    <TextBlock FontSize="11" Foreground="{DynamicResource MaterialDesignBodyLight}">
                                        <Run Text="{DynamicResource Loc.TL.HistoricalLabel}"/>
                                        <Run Text="{Binding HistoryCount, Mode=OneWay}" FontWeight="Medium"/>
                                    </TextBlock>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock FontSize="11" Foreground="{DynamicResource MaterialDesignBodyLight}">
                                        <Run Text="{DynamicResource Loc.TL.PlotLabel}"/>
                                        <Run Text="{Binding PlotCount, Mode=OneWay}" FontWeight="Medium"/>
                                    </TextBlock>
                                    <TextBlock FontSize="11" Foreground="{DynamicResource MaterialDesignBodyLight}">
                                        <Run Text="{DynamicResource Loc.TL.CharacterLabel}"/>
                                        <Run Text="{Binding CharacterCount, Mode=OneWay}" FontWeight="Medium"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </materialDesign:Card>

            <!-- 右侧：事件详情编辑区域 -->
            <materialDesign:Card Grid.Column="1" Margin="12,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="DetailPanel" Margin="24">
                        <!-- 默认显示：时间线统计信息 -->
                        <StackPanel x:Name="EmptyStatePanel" Visibility="Visible">
                            <TextBlock Text="{DynamicResource Loc.TL.Stats}"
                                     FontSize="24"
                                     FontWeight="Medium"
                                     Margin="0,0,0,24"/>

                            <!-- 统计卡片 -->
                            <UniformGrid Columns="2" Margin="0,0,0,24">
                                <materialDesign:Card Margin="0,0,8,8" Padding="16">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource Loc.TL.TotalCount}"
                                                 FontSize="14"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                        <TextBlock x:Name="TotalEventsText"
                                                 Text="0"
                                                 FontSize="32"
                                                 FontWeight="Bold"/>
                                    </StackPanel>
                                </materialDesign:Card>

                                <materialDesign:Card Margin="8,0,0,8" Padding="16">
                                    <StackPanel>
                                        <TextBlock Tag="历史事件" Text="{DynamicResource Loc.TL.Data.历史事件}"
                                                 FontSize="14"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                        <TextBlock x:Name="HistoryEventsText"
                                                 Text="0"
                                                 FontSize="32"
                                                 FontWeight="Bold"/>
                                    </StackPanel>
                                </materialDesign:Card>

                                <materialDesign:Card Margin="0,8,8,0" Padding="16">
                                    <StackPanel>
                                        <TextBlock Tag="剧情事件" Text="{DynamicResource Loc.TL.Data.剧情事件}"
                                                 FontSize="14"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                        <TextBlock x:Name="PlotEventsText"
                                                 Text="0"
                                                 FontSize="32"
                                                 FontWeight="Bold"/>
                                    </StackPanel>
                                </materialDesign:Card>

                                <materialDesign:Card Margin="8,8,0,0" Padding="16">
                                    <StackPanel>
                                        <TextBlock Tag="角色事件" Text="{DynamicResource Loc.TL.Data.角色事件}"
                                                 FontSize="14"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                        <TextBlock x:Name="CharacterEventsText"
                                                 Text="0"
                                                 FontSize="32"
                                                 FontWeight="Bold"/>
                                    </StackPanel>
                                </materialDesign:Card>
                            </UniformGrid>

                            <!-- 事件状态分布 -->
                            <TextBlock Text="{DynamicResource Loc.TL.StatusDistribution}"
                                     FontSize="18"
                                     FontWeight="Medium"
                                     Margin="0,0,0,16"/>

                            <ItemsControl>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <materialDesign:Chip Grid.Column="0" Tag="已完成" Content="{DynamicResource Loc.TL.Data.已完成}" Margin="0,0,12,0" Background="#4CAF50"/>
                                    <ProgressBar Grid.Column="1" x:Name="CompletedProgressBar" Maximum="100" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" x:Name="CompletedCountText" Text="0" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <materialDesign:Chip Grid.Column="0" Tag="进行中" Content="{DynamicResource Loc.TL.Data.进行中}" Margin="0,0,12,0" Background="#2196F3"/>
                                    <ProgressBar Grid.Column="1" x:Name="InProgressProgressBar" Maximum="100" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" x:Name="InProgressCountText" Text="0" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <materialDesign:Chip Grid.Column="0" Tag="计划中" Content="{DynamicResource Loc.TL.Data.计划中}" Margin="0,0,12,0" Background="#FF9800"/>
                                    <ProgressBar Grid.Column="1" x:Name="PlannedProgressBar" Maximum="100" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" x:Name="PlannedCountText" Text="0" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </Grid>
                            </ItemsControl>

                            <!-- 最近事件 -->
                            <TextBlock Text="{DynamicResource Loc.TL.RecentEvents}"
                                     FontSize="18"
                                     FontWeight="Medium"
                                     Margin="0,24,0,16"/>

                            <StackPanel x:Name="RecentEventsPanel">
                                <TextBlock Text="{DynamicResource Loc.TL.NoData}"
                                         FontSize="14"
                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                         HorizontalAlignment="Center"
                                         Margin="0,16,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- 事件详情编辑面板 -->
                        <StackPanel x:Name="EditPanel" Visibility="Collapsed">
                            <!-- 基本信息 -->
                            <TextBlock Text="{DynamicResource Loc.TL.BasicInfo}"
                                     FontSize="18"
                                     FontWeight="Medium"
                                     Margin="0,0,0,16"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0"
                                        x:Name="EventTitleTextBox"
                                        materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.EventTitleHint}"
                                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                        Margin="0,0,0,16"/>

                                <ComboBox Grid.Column="2"
                                         x:Name="EventCategoryComboBox"
                                         materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.CategoryHint}"
                                         Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                         Margin="0,0,0,16">
                                    <ComboBoxItem Tag="历史事件" Content="{DynamicResource Loc.TL.Data.历史事件}"/>
                                    <ComboBoxItem Tag="剧情事件" Content="{DynamicResource Loc.TL.Data.剧情事件}"/>
                                    <ComboBoxItem Tag="角色事件" Content="{DynamicResource Loc.TL.Data.角色事件}"/>
                                    <ComboBoxItem Tag="势力事件" Content="{DynamicResource Loc.TL.Data.势力事件}"/>
                                    <ComboBoxItem Tag="世界事件" Content="{DynamicResource Loc.TL.Data.世界事件}"/>
                                    <ComboBoxItem Tag="修炼事件" Content="{DynamicResource Loc.TL.Data.修炼事件}"/>
                                </ComboBox>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <DatePicker Grid.Column="0"
                                           x:Name="EventDatePicker"
                                           materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.EventDateHint}"
                                           Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                           Margin="0,0,0,16"/>

                                <TextBox Grid.Column="2"
                                        x:Name="EventLocationTextBox"
                                        materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.EventLocationHint}"
                                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                        Margin="0,0,0,16"/>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <ComboBox Grid.Column="0"
                                         x:Name="EventImportanceComboBox"
                                         materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.ImportanceHint}"
                                         Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                         Margin="0,0,0,16">
                                    <ComboBoxItem Tag="低" Content="{DynamicResource Loc.TL.Data.低}"/>
                                    <ComboBoxItem Tag="中" Content="{DynamicResource Loc.TL.Data.中}"/>
                                    <ComboBoxItem Tag="高" Content="{DynamicResource Loc.TL.Data.高}"/>
                                    <ComboBoxItem Tag="极高" Content="{DynamicResource Loc.TL.Data.极高}"/>
                                </ComboBox>

                                <ComboBox Grid.Column="2"
                                         x:Name="EventStatusComboBox"
                                         materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.StatusHint}"
                                         Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                         Margin="0,0,0,16">
                                    <ComboBoxItem Tag="计划中" Content="{DynamicResource Loc.TL.Data.计划中}"/>
                                    <ComboBoxItem Tag="进行中" Content="{DynamicResource Loc.TL.Data.进行中}"/>
                                    <ComboBoxItem Tag="已完成" Content="{DynamicResource Loc.TL.Data.已完成}"/>
                                    <ComboBoxItem Tag="已取消" Content="{DynamicResource Loc.TL.Data.已取消}"/>
                                </ComboBox>
                            </Grid>

                            <TextBox x:Name="EventDescriptionTextBox"
                                    materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.DescriptionHint}"
                                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                    AcceptsReturn="True"
                                    TextWrapping="Wrap"
                                    MinLines="3"
                                    MaxLines="5"
                                    Margin="0,0,0,24"/>

                            <!-- 参与者管理 -->
                            <TextBlock Text="{DynamicResource Loc.TL.ParticipantMgmt}"
                                     FontSize="18"
                                     FontWeight="Medium"
                                     Margin="0,0,0,16"/>

                            <Border BorderBrush="{DynamicResource MaterialDesignDivider}"
                                  BorderThickness="1"
                                  CornerRadius="4"
                                  Padding="16"
                                  Margin="0,0,0,24">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                 Text="{DynamicResource Loc.TL.ParticipantList}"
                                                 FontWeight="Medium"/>

                                        <Button Grid.Column="1"
                                              Style="{StaticResource MaterialDesignIconButton}"
                                              Click="AddParticipant_Click"
                                              ToolTip="{DynamicResource Loc.TL.AddParticipantTooltip}">
                                            <materialDesign:PackIcon Kind="Plus"/>
                                        </Button>
                                    </Grid>

                                    <ItemsControl x:Name="ParticipantListControl" Margin="0,8,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="{DynamicResource MaterialDesignDivider}"
                                                      BorderThickness="0,0,0,1"
                                                      Padding="0,8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <TextBox Grid.Column="0"
                                                               Text="{Binding Name}"
                                                               materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.ParticipantNameHint}"
                                                               Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                               Margin="0,0,8,0"/>

                                                        <ComboBox Grid.Column="1"
                                                                 Text="{Binding Type}"
                                                                 materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.ParticipantTypeHint}"
                                                                 Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                                                 Margin="0,0,8,0">
                                                            <ComboBoxItem Tag="角色" Content="{DynamicResource Loc.TL.Data.角色}"/>
                                                            <ComboBoxItem Tag="势力" Content="{DynamicResource Loc.TL.Data.势力}"/>
                                                            <ComboBoxItem Tag="组织" Content="{DynamicResource Loc.TL.Data.组织}"/>
                                                            <ComboBoxItem Tag="其他" Content="{DynamicResource Loc.TL.Data.其他}"/>
                                                        </ComboBox>

                                                        <TextBox Grid.Column="2"
                                                               Text="{Binding Role}"
                                                               materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.ParticipantCharsHint}"
                                                               Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                               Margin="0,0,8,0"/>

                                                        <Button Grid.Column="3"
                                                              Style="{StaticResource MaterialDesignIconButton}"
                                                              Click="RemoveParticipant_Click"
                                                              CommandParameter="{Binding}"
                                                              ToolTip="{DynamicResource Loc.TL.DeleteParticipantTooltip}">
                                                            <materialDesign:PackIcon Kind="Delete"
                                                                                   Foreground="{DynamicResource ValidationErrorBrush}"/>
                                                        </Button>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <!-- 影响和结果 -->
                            <TextBlock Text="{DynamicResource Loc.TL.ImpactTitle}"
                                     FontSize="18"
                                     FontWeight="Medium"
                                     Margin="0,0,0,16"/>

                            <TextBox x:Name="EventImpactTextBox"
                                    materialDesign:HintAssist.Hint="{DynamicResource Loc.TL.ImpactHint}"
                                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                    AcceptsReturn="True"
                                    TextWrapping="Wrap"
                                    MinLines="3"
                                    MaxLines="5"
                                    Margin="0,0,0,24"/>

                            <!-- 操作按钮 -->
                            <StackPanel Orientation="Horizontal"
                                      HorizontalAlignment="Right"
                                      Margin="0,16,0,0">
                                <Button Content="{DynamicResource Loc.Dlg.Save}"
                                      Style="{StaticResource MaterialDesignRaisedButton}"
                                      Click="SaveEvent_Click"
                                      Margin="0,0,8,0"/>
                                <Button Content="{DynamicResource Loc.Dlg.Cancel}"
                                      Style="{StaticResource MaterialDesignOutlinedButton}"
                                      Click="CancelEdit_Click"
                                      Margin="8,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>

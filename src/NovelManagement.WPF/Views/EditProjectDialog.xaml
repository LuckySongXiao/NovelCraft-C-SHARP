<Window x:Class="NovelManagement.WPF.Views.EditProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="编辑项目" Height="500" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <materialDesign:ColorZone Grid.Row="0" 
                                  Mode="PrimaryMid" 
                                  Padding="16">
            <DockPanel>
                <materialDesign:PackIcon Kind="Edit"
                                       DockPanel.Dock="Left"
                                       Width="24" Height="24"
                                       VerticalAlignment="Center"
                                       Margin="0,0,16,0"/>
                <TextBlock Text="编辑项目" 
                         FontSize="20" 
                         FontWeight="Medium" 
                         VerticalAlignment="Center"/>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- 表单内容 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24">
            <StackPanel>
                <!-- 项目基本信息 -->
                <materialDesign:Card Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="项目基本信息" 
                                 FontSize="18" 
                                 FontWeight="Medium" 
                                 Margin="0,0,0,16"/>

                        <!-- 项目名称 -->
                        <TextBox x:Name="ProjectNameTextBox"
                               materialDesign:HintAssist.Hint="项目名称 *"
                               materialDesign:HintAssist.IsFloating="True"
                               Margin="0,0,0,16"/>

                        <!-- 项目描述 -->
                        <TextBox x:Name="ProjectDescriptionTextBox"
                               materialDesign:HintAssist.Hint="项目描述"
                               materialDesign:HintAssist.IsFloating="True"
                               AcceptsReturn="True"
                               TextWrapping="Wrap"
                               Height="80"
                               VerticalScrollBarVisibility="Auto"
                               Margin="0,0,0,16"/>

                        <!-- 项目类型 -->
                        <ComboBox x:Name="ProjectTypeComboBox"
                                materialDesign:HintAssist.Hint="项目类型 *"
                                materialDesign:HintAssist.IsFloating="True"
                                Margin="0,0,0,16">
                            <ComboBoxItem Content="修仙小说"/>
                            <ComboBoxItem Content="都市小说"/>
                            <ComboBoxItem Content="科幻小说"/>
                            <ComboBoxItem Content="奇幻小说"/>
                            <ComboBoxItem Content="历史小说"/>
                            <ComboBoxItem Content="军事小说"/>
                            <ComboBoxItem Content="悬疑小说"/>
                            <ComboBoxItem Content="言情小说"/>
                            <ComboBoxItem Content="其他"/>
                        </ComboBox>

                        <!-- 项目状态 -->
                        <ComboBox x:Name="ProjectStatusComboBox"
                                materialDesign:HintAssist.Hint="项目状态"
                                materialDesign:HintAssist.IsFloating="True"
                                Margin="0,0,0,0">
                            <ComboBoxItem Content="进行中"/>
                            <ComboBoxItem Content="暂停"/>
                            <ComboBoxItem Content="已完成"/>
                            <ComboBoxItem Content="已取消"/>
                        </ComboBox>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>

        <!-- 按钮栏 -->
        <Border Grid.Row="2" 
              Background="{DynamicResource MaterialDesignDivider}" 
              Padding="24,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" 
                      Style="{StaticResource MaterialDesignFlatButton}"
                      Margin="0,0,8,0"
                      Click="Cancel_Click"/>
                <Button Content="保存更改" 
                      Style="{StaticResource MaterialDesignRaisedButton}"
                      Click="SaveProject_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

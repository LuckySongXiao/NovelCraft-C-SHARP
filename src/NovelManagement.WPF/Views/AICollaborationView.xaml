<UserControl x:Class="NovelManagement.WPF.Views.AICollaborationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 页面标题 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="{DynamicResource Loc.AC.Title}"
                     FontSize="32"
                     FontWeight="Bold"
                     Margin="0,0,0,8"/>
            <TextBlock Text="{DynamicResource Loc.AC.Subtitle}"
                     FontSize="16"
                     Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>

        <!-- 主要内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧面板 - Agent状态监控 -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Agent状态监控标题 -->
                <materialDesign:Card Grid.Row="0" Padding="16" Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Robot"
                                               VerticalAlignment="Center"
                                               Width="24" Height="24"
                                               Margin="0,0,8,0"/>
                        <TextBlock Text="{DynamicResource Loc.AC.AgentMonitor}"
                                 FontSize="18"
                                 FontWeight="Medium"
                                 VerticalAlignment="Center"/>
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                              ToolTip="{DynamicResource Loc.AC.RefreshAgentStatus}"
                              Margin="8,0,0,0"
                              Click="RefreshAgents_Click">
                            <materialDesign:PackIcon Kind="Refresh"/>
                        </Button>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Agent列表 -->
                <materialDesign:Card Grid.Row="1" Padding="0">
                    <ListView x:Name="AgentListView"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="16,12"
                                      BorderBrush="{DynamicResource MaterialDesignDivider}"
                                      BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Agent图标 -->
                                        <materialDesign:PackIcon Grid.Column="0"
                                                               Kind="{Binding IconKind}"
                                                               Width="32" Height="32"
                                                               VerticalAlignment="Center"
                                                               Foreground="{Binding StatusColor}"/>

                                        <!-- Agent信息 -->
                                        <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                            <TextBlock Text="{Binding Name}"
                                                     FontWeight="Medium"
                                                     FontSize="14"/>
                                            <TextBlock Text="{Binding StatusDescription}"
                                                     FontSize="12"
                                                     Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                            <TextBlock Text="{Binding CurrentTask}"
                                                     FontSize="11"
                                                     Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                     Visibility="{Binding HasCurrentTask, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                            <!-- 进度条 -->
                                            <ProgressBar Value="{Binding Progress}"
                                                       Maximum="100"
                                                       Height="4"
                                                       Margin="0,4,0,0"
                                                       Visibility="{Binding IsWorking, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </StackPanel>

                                        <!-- 操作按钮 -->
                                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                  ToolTip="{DynamicResource Loc.AC.ViewDetails}"
                                                  Click="ViewAgentDetails_Click"
                                                  Tag="{Binding}">
                                                <materialDesign:PackIcon Kind="Information"/>
                                            </Button>
                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                  ToolTip="{DynamicResource Loc.AC.ResetAgent}"
                                                  Click="ResetAgent_Click"
                                                  Tag="{Binding}">
                                                <materialDesign:PackIcon Kind="Restart"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </materialDesign:Card>

                <!-- 工作流控制面板标题 -->
                <materialDesign:Card Grid.Row="3" Padding="16" Margin="0,8,0,8">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Workflow"
                                               VerticalAlignment="Center"
                                               Width="24" Height="24"
                                               Margin="0,0,8,0"/>
                        <TextBlock Text="{DynamicResource Loc.AC.WorkflowControl}"
                                 FontSize="18"
                                 FontWeight="Medium"
                                 VerticalAlignment="Center"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- 工作流控制按钮 -->
                <materialDesign:Card Grid.Row="4" Padding="16">
                    <StackPanel>
                        <Button Content="{DynamicResource Loc.AC.RunProjectInitWorkflow}"
                              Style="{StaticResource MaterialDesignRaisedButton}"
                              Margin="0,0,0,8"
                              Click="CreateProjectInitWorkflow_Click"/>
                        <Button Content="{DynamicResource Loc.AC.RunChapterWorkflow}"
                              Style="{StaticResource MaterialDesignRaisedButton}"
                              Margin="0,0,0,8"
                              Click="CreateChapterWorkflow_Click"/>
                        <Button Content="{DynamicResource Loc.AC.RunReviewWorkflow}"
                              Style="{StaticResource MaterialDesignRaisedButton}"
                              Margin="0,0,0,8"
                              Click="CreateReviewWorkflow_Click"/>
                        <Button Content="{DynamicResource Loc.AC.RunConsistencyWorkflow}"
                              Style="{StaticResource MaterialDesignRaisedButton}"
                              Margin="0,0,0,0"
                              Click="CreateConsistencyWorkflow_Click"/>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>

            <!-- 分隔符 -->
            <GridSplitter Grid.Column="1"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Background="{DynamicResource MaterialDesignDivider}"/>

            <!-- 中间面板 - DeepSeek API配置和思维链控制 -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- AI模型配置标题 -->
                <materialDesign:Card Grid.Row="0" Padding="16" Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Robot"
                                               VerticalAlignment="Center"
                                               Width="24" Height="24"
                                               Margin="0,0,8,0"/>
                        <TextBlock Text="{DynamicResource Nav.AIConfiguration}"
                                 FontSize="18"
                                 FontWeight="Medium"
                                 VerticalAlignment="Center"/>
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                              ToolTip="{DynamicResource Loc.AC.OpenConfigWindow}"
                              Margin="8,0,0,0"
                              Click="OpenConfigWindow_Click">
                            <materialDesign:PackIcon Kind="Settings"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                              ToolTip="{DynamicResource Loc.AC.TestConnection}"
                              Margin="4,0,0,0"
                              Click="TestConnection_Click">
                            <materialDesign:PackIcon Kind="TestTube"/>
                        </Button>
                    </StackPanel>
                </materialDesign:Card>

                <!-- AI模型配置面板 -->
                <materialDesign:Card Grid.Row="1" Padding="16">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- 提供者选择 -->
                            <ComboBox x:Name="ProviderComboBox"
                                    materialDesign:HintAssist.Hint="{DynamicResource Loc.AC.AIProvider}"
                                    Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                    SelectionChanged="ProviderComboBox_SelectionChanged"
                                    Margin="0,0,0,16">
                                <ComboBoxItem Content="{DynamicResource Loc.AC.ProviderOllamaLocal}" Tag="Ollama" IsSelected="True"/>
                                <ComboBoxItem Content="{DynamicResource Loc.AC.ProviderDeepSeekCloud}" Tag="DeepSeek"/>
                                <ComboBoxItem Content="{DynamicResource Loc.AC.ProviderMCP}" Tag="MCP"/>
                            </ComboBox>

                            <!-- 模型选择 -->
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ComboBox x:Name="ModelComboBox"
                                        Grid.Column="0"
                                        materialDesign:HintAssist.Hint="{DynamicResource Loc.AC.SelectModel}"
                                        Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                        Margin="0,0,8,0"/>

                                <Button x:Name="RefreshModelsButton"
                                      Grid.Column="1"
                                      Style="{StaticResource MaterialDesignIconButton}"
                                      ToolTip="{DynamicResource Loc.AC.RefreshModelList}"
                                      Click="RefreshModels_Click">
                                    <materialDesign:PackIcon Kind="Refresh"/>
                                </Button>
                            </Grid>

                            <!-- 连接状态 -->
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <materialDesign:PackIcon x:Name="ConnectionStatusIcon"
                                                       Grid.Column="0"
                                                       Kind="Circle"
                                                       VerticalAlignment="Center"
                                                       Foreground="Orange"
                                                       Margin="0,0,8,0"/>

                                <TextBlock x:Name="ConnectionStatusText"
                                         Grid.Column="1"
                                         Text="{DynamicResource Loc.AC.Checking}"
                                         VerticalAlignment="Center"/>

                                <Button x:Name="RefreshConnectionButton"
                                      Grid.Column="2"
                                      Content="{DynamicResource Dlg.Refresh}"
                                      Style="{StaticResource MaterialDesignOutlinedButton}"
                                      Click="RefreshConnection_Click"/>
                            </Grid>

                            <!-- 温度参数 -->
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Slider x:Name="TemperatureSlider"
                                      Grid.Column="0"
                                      Minimum="0" Maximum="2" Value="0.7"
                                      TickFrequency="0.1" IsSnapToTickEnabled="True"
                                      VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                                    <TextBlock Text="{DynamicResource Loc.AC.TemperatureLabel}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Text="{Binding Value, ElementName=TemperatureSlider, StringFormat={}{0:F1}}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>

                            <!-- 最大令牌数 -->
                            <TextBox x:Name="MaxTokensTextBox"
                                   materialDesign:HintAssist.Hint="{DynamicResource Loc.AC.MaxTokens}"
                                   Text="4000"
                                   Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                   Margin="0,0,0,16"/>

                            <!-- 启用思维链 -->
                            <CheckBox x:Name="EnableThinkingChainCheckBox"
                                    Content="{DynamicResource Loc.AC.EnableThinkingChain}"
                                    IsChecked="True"
                                    Margin="0,0,0,16"/>

                            <!-- 启用流式响应 -->
                            <CheckBox x:Name="EnableStreamingCheckBox"
                                    Content="{DynamicResource Loc.AC.EnableStreaming}"
                                    IsChecked="True"
                                    Margin="0,0,0,16"/>

                            <!-- 快速测试区域 -->
                            <Expander Header="{DynamicResource Loc.AC.QuickTest}" Margin="0,16,0,0">
                                <StackPanel Margin="0,8,0,0">
                                    <TextBox x:Name="TestPromptTextBox"
                                           materialDesign:HintAssist.Hint="{DynamicResource Loc.AC.TestPromptHint}"
                                           Text="{DynamicResource Loc.AC.TestPromptDefault}"
                                           Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                           AcceptsReturn="True"
                                           TextWrapping="Wrap"
                                           MinHeight="60"
                                           Margin="0,0,0,8"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Button x:Name="SendTestButton"
                                              Grid.Column="0"
                                              Content="{DynamicResource Loc.AC.SendTest}"
                                              Style="{StaticResource MaterialDesignRaisedButton}"
                                              Click="SendTest_Click"
                                              Margin="0,0,8,0"/>

                                        <CheckBox x:Name="StreamingTestCheckBox"
                                                Grid.Column="1"
                                                Content="{DynamicResource Loc.AC.StreamingResponse}"
                                                IsChecked="True"
                                                VerticalAlignment="Center"/>
                                    </Grid>

                                    <TextBox x:Name="TestResponseTextBox"
                                           materialDesign:HintAssist.Hint="{DynamicResource Loc.AC.ResponsePlaceholder}"
                                           Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                           AcceptsReturn="True"
                                           TextWrapping="Wrap"
                                           IsReadOnly="True"
                                           MinHeight="120"
                                           Margin="0,8,0,0"/>
                                </StackPanel>
                            </Expander>

                            <!-- 保存配置按钮 -->
                            <Button Content="{DynamicResource Loc.AC.SaveConfig}"
                                  Style="{StaticResource MaterialDesignRaisedButton}"
                                  Margin="0,16,0,0"
                                  Click="SaveConfiguration_Click"/>
                        </StackPanel>
                    </ScrollViewer>
                </materialDesign:Card>

                <!-- 思维链控制标题 -->
                <materialDesign:Card Grid.Row="3" Padding="16" Margin="0,8,0,8">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Brain"
                                               VerticalAlignment="Center"
                                               Width="24" Height="24"
                                               Margin="0,0,8,0"/>
                        <TextBlock Text="{DynamicResource Loc.AC.ThinkingChainControl}"
                                 FontSize="18"
                                 FontWeight="Medium"
                                 VerticalAlignment="Center"/>
                        <TextBlock x:Name="ActiveThinkingChainsText"
                                 Text="{DynamicResource Loc.AC.ActiveWindows}"
                                 FontSize="12"
                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                 VerticalAlignment="Center"
                                 Margin="8,0,0,0"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- 思维链控制面板 -->
                <materialDesign:Card Grid.Row="4" Padding="16">
                    <StackPanel>
                        <!-- 悬浮窗口设置 -->
                        <TextBlock Text="{DynamicResource Loc.AC.FloatingWindowSettings}" FontWeight="Medium" Margin="0,0,0,8"/>

                        <!-- 透明度 -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Slider x:Name="OpacitySlider"
                                  Grid.Column="0"
                                  Minimum="0.1" Maximum="1.0" Value="0.9"
                                  TickFrequency="0.1" IsSnapToTickEnabled="True"
                                  VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                                <TextBlock Text="{DynamicResource Loc.AC.OpacityLabel}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <TextBlock Text="{Binding Value, ElementName=OpacitySlider, StringFormat={}{0:P0}}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <!-- 默认位置 -->
                        <ComboBox x:Name="DefaultPositionComboBox"
                                materialDesign:HintAssist.Hint="{DynamicResource Loc.AC.DefaultPosition}"
                                Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                Margin="0,0,0,12">
                            <ComboBoxItem Content="{DynamicResource Loc.AC.PosTopRight}" Tag="TopRight" IsSelected="True"/>
                            <ComboBoxItem Content="{DynamicResource Loc.AC.PosTopLeft}" Tag="TopLeft"/>
                            <ComboBoxItem Content="{DynamicResource Loc.AC.PosBottomRight}" Tag="BottomRight"/>
                            <ComboBoxItem Content="{DynamicResource Loc.AC.PosBottomLeft}" Tag="BottomLeft"/>
                            <ComboBoxItem Content="{DynamicResource Loc.AC.PosCenter}" Tag="Center"/>
                        </ComboBox>

                        <!-- 最大窗口数 -->
                        <TextBox x:Name="MaxWindowsTextBox"
                               materialDesign:HintAssist.Hint="{DynamicResource Loc.AC.MaxWindows}"
                               Text="3"
                               Style="{StaticResource MaterialDesignOutlinedTextBox}"
                               Margin="0,0,0,16"/>

                        <!-- 控制按钮 -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="{DynamicResource Loc.AC.HideAll}"
                                  Style="{StaticResource MaterialDesignOutlinedButton}"
                                  Margin="0,0,8,0"
                                  Click="HideAllThinkingChains_Click"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>

            <!-- 分隔符 -->
            <GridSplitter Grid.Column="3"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Background="{DynamicResource MaterialDesignDivider}"/>

            <!-- 右侧面板 - 任务队列、统计监控和日志 -->
            <Grid Grid.Column="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 任务队列标题 -->
                <materialDesign:Card Grid.Row="0" Padding="16" Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FormatListBulleted"
                                               VerticalAlignment="Center"
                                               Width="24" Height="24"
                                               Margin="0,0,8,0"/>
                        <TextBlock Text="{DynamicResource Loc.AC.TaskQueue}"
                                 FontSize="18"
                                 FontWeight="Medium"
                                 VerticalAlignment="Center"/>
                        <TextBlock x:Name="TaskQueueStatusText"
                                 Text="{DynamicResource Loc.AC.TaskQueueStatus}"
                                 FontSize="12"
                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                 VerticalAlignment="Center"
                                 Margin="8,0,0,0"/>
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                              ToolTip="{DynamicResource Loc.AC.ClearQueue}"
                              Margin="8,0,0,0"
                              Click="ClearQueue_Click">
                            <materialDesign:PackIcon Kind="DeleteSweep"/>
                        </Button>
                    </StackPanel>
                </materialDesign:Card>

                <!-- 任务列表 -->
                <materialDesign:Card Grid.Row="1" Padding="0">
                    <ListView x:Name="TaskQueueListView"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="16,12"
                                      BorderBrush="{DynamicResource MaterialDesignDivider}"
                                      BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 任务状态图标 -->
                                        <materialDesign:PackIcon Grid.Column="0"
                                                               Kind="{Binding StatusIcon}"
                                                               Width="24" Height="24"
                                                               VerticalAlignment="Center"
                                                               Foreground="{Binding StatusColor}"/>

                                        <!-- 任务信息 -->
                                        <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                            <TextBlock Text="{Binding Name}"
                                                     FontWeight="Medium"
                                                     FontSize="14"/>
                                            <TextBlock Text="{Binding TaskType}"
                                                     FontSize="12"
                                                     Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource Loc.AC.TargetAgentLabel}"
                                                         FontSize="11"
                                                         Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                                <TextBlock Text="{Binding TargetAgentId}"
                                                         FontSize="11"
                                                         Margin="4,0,0,0"
                                                         Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                            </StackPanel>

                                            <!-- 进度条 -->
                                            <ProgressBar Value="{Binding Progress}"
                                                       Maximum="100"
                                                       Height="4"
                                                       Margin="0,4,0,0"
                                                       Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </StackPanel>

                                        <!-- 操作按钮 -->
                                        <Button Grid.Column="2"
                                              Style="{StaticResource MaterialDesignIconButton}"
                                              ToolTip="{DynamicResource Loc.AC.CancelTask}"
                                              Click="CancelTask_Click"
                                              Tag="{Binding}"
                                              Visibility="{Binding CanCancel, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <materialDesign:PackIcon Kind="Cancel"/>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </materialDesign:Card>

                <!-- 统计监控标题 -->
                <materialDesign:Card Grid.Row="3" Padding="16" Margin="0,8,0,8">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ChartLine"
                                               VerticalAlignment="Center"
                                               Width="24" Height="24"
                                               Margin="0,0,8,0"/>
                        <TextBlock Text="{DynamicResource Loc.AC.UsageStats}"
                                 FontSize="18"
                                 FontWeight="Medium"
                                 VerticalAlignment="Center"/>
                        <ComboBox x:Name="StatisticsTimeRangeComboBox"
                                Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                Width="120"
                                Margin="16,0,8,0"
                                SelectionChanged="StatisticsTimeRange_SelectionChanged">
                            <ComboBoxItem Content="{DynamicResource Loc.AC.Today}" Tag="Today" IsSelected="True"/>
                            <ComboBoxItem Content="{DynamicResource Loc.AC.Yesterday}" Tag="Yesterday"/>
                            <ComboBoxItem Content="{DynamicResource Loc.AC.ThisWeek}" Tag="LastWeek"/>
                            <ComboBoxItem Content="{DynamicResource Loc.AC.ThisMonth}" Tag="LastMonth"/>
                        </ComboBox>
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                              ToolTip="{DynamicResource Loc.AC.RefreshStats}"
                              Margin="0,0,8,0"
                              Click="RefreshStatistics_Click">
                            <materialDesign:PackIcon Kind="Refresh"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                              ToolTip="{DynamicResource Loc.AC.ExportStats}"
                              Click="ExportStatistics_Click">
                            <materialDesign:PackIcon Kind="Export"/>
                        </Button>
                    </StackPanel>
                </materialDesign:Card>

                <!-- 统计监控面板 -->
                <materialDesign:Card Grid.Row="4" Padding="16">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- 基础统计 -->
                            <TextBlock Text="{DynamicResource Loc.AC.BasicStats}" FontWeight="Medium" Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
                                    <TextBlock Text="{DynamicResource Loc.AC.TotalRequests}" FontSize="12" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    <TextBlock x:Name="TotalRequestsText" Text="0" FontSize="18" FontWeight="Medium"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,8">
                                    <TextBlock Text="{DynamicResource Loc.AC.SuccessRate}" FontSize="12" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    <TextBlock x:Name="SuccessRateText" Text="0%" FontSize="18" FontWeight="Medium"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,8">
                                    <TextBlock Text="{DynamicResource Loc.AC.AvgTime}" FontSize="12" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    <TextBlock x:Name="AverageTimeText" Text="0s" FontSize="18" FontWeight="Medium"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,8">
                                    <TextBlock Text="{DynamicResource Loc.AC.TokenUsage}" FontSize="12" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    <TextBlock x:Name="TokenUsageText" Text="0" FontSize="18" FontWeight="Medium"/>
                                </StackPanel>
                            </Grid>

                            <!-- 功能使用排行 -->
                            <TextBlock Text="{DynamicResource Loc.AC.FunctionRanking}" FontWeight="Medium" Margin="0,0,0,8"/>
                            <ListView x:Name="FunctionRankingListView"
                                    Height="120"
                                    Margin="0,0,0,16">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="{DynamicResource Loc.AC.ColFunction}" Width="100" DisplayMemberBinding="{Binding FunctionName}"/>
                                        <GridViewColumn Header="{DynamicResource Loc.AC.ColCount}" Width="50" DisplayMemberBinding="{Binding UsageCount}"/>
                                        <GridViewColumn Header="{DynamicResource Loc.AC.SuccessRate}" Width="70" DisplayMemberBinding="{Binding SuccessRate, StringFormat={}{0:F1}%}"/>
                                    </GridView>
                                </ListView.View>
                            </ListView>

                            <!-- 最近错误 -->
                            <TextBlock Text="{DynamicResource Loc.AC.RecentErrors}" FontWeight="Medium" Margin="0,0,0,8"/>
                            <ListView x:Name="RecentErrorsListView" Height="80">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="{DynamicResource Loc.AC.ColTime}" Width="80" DisplayMemberBinding="{Binding Timestamp, StringFormat={}{0:HH:mm}}"/>
                                        <GridViewColumn Header="{DynamicResource Loc.AC.ColFunction}" Width="80" DisplayMemberBinding="{Binding FunctionName}"/>
                                        <GridViewColumn Header="{DynamicResource Loc.AC.ColError}" Width="120" DisplayMemberBinding="{Binding ErrorMessage}"/>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </StackPanel>
                    </ScrollViewer>
                </materialDesign:Card>

                <!-- 交互日志标题 -->
                <materialDesign:Card Grid.Row="6" Padding="16" Margin="0,8,0,8">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="MessageText"
                                               VerticalAlignment="Center"
                                               Width="24" Height="24"
                                               Margin="0,0,8,0"/>
                        <TextBlock Text="{DynamicResource Loc.AC.AgentLog}"
                                 FontSize="18"
                                 FontWeight="Medium"
                                 VerticalAlignment="Center"/>
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                              ToolTip="{DynamicResource Loc.AC.ClearLog}"
                              Margin="8,0,0,0"
                              Click="ClearLog_Click">
                            <materialDesign:PackIcon Kind="Delete"/>
                        </Button>
                    </StackPanel>
                </materialDesign:Card>

                <!-- 日志列表 -->
                <materialDesign:Card Grid.Row="7" Padding="8">
                    <ScrollViewer x:Name="LogScrollViewer"
                                VerticalScrollBarVisibility="Auto"
                                HorizontalScrollBarVisibility="Disabled">
                        <TextBlock x:Name="LogTextBlock"
                                 FontFamily="Consolas"
                                 FontSize="11"
                                 TextWrapping="Wrap"
                                 Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    </ScrollViewer>
                </materialDesign:Card>
            </Grid>
        </Grid>
    </Grid>
</UserControl>

<Window x:Class="NovelManagement.WPF.Views.PasswordGateWindow" Icon="pack://application:,,,/Assets/icon.ico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="{DynamicResource Loc.PGate.WindowTitle}"
        Width="420" Height="260"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource RegionBrush}">
    <Grid Margin="28,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{DynamicResource Loc.PGate.AppName}" FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
        <TextBlock Grid.Row="1" Text="{DynamicResource Loc.PGate.Subtitle}" FontSize="13" Margin="0,6,0,16"
                   HorizontalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}"/>

        <PasswordBox x:Name="PasswordInput" Grid.Row="2" Height="34" FontSize="14"
                     VerticalContentAlignment="Center" PasswordChar="●"
                     KeyDown="PasswordInput_KeyDown"/>

        <TextBlock x:Name="ErrorText" Grid.Row="3" Text="" FontSize="12" Margin="0,8,0,0"
                   Foreground="#E53935" HorizontalAlignment="Center" Visibility="Collapsed"/>

        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="ConfirmButton" Grid.Column="0" Content="{DynamicResource Loc.PGate.Verify}" Height="34" FontSize="14"
                    Click="Confirm_Click"/>
            <Button x:Name="CancelButton" Grid.Column="2" Content="{DynamicResource Loc.PGate.Cancel}" Height="34" FontSize="14"
                    IsCancel="True" Click="Cancel_Click"/>
        </Grid>
    </Grid>
</Window>

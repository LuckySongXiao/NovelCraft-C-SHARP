<UserControl x:Class="NovelManagement.WPF.Views.FactionManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 页面标题 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="{DynamicResource Loc.FM.Title}" 
                     FontSize="32" 
                     FontWeight="Bold" 
                     Margin="0,0,0,8"/>
            <TextBlock Text="{DynamicResource Loc.FM.Subtitle}" 
                     FontSize="16" 
                     Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>

        <!-- 操作工具栏 -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Margin="0,0,12,0"
                            Click="NewFaction_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Loc.FM.New}"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="0,0,12,0"
                            Click="ImportFactions_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Import" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Loc.FM.Import}"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="0,0,12,0"
                            Click="ExportFactions_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Export" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Loc.FM.Export}"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="0,0,12,0"
                            Click="ViewRelationshipNetwork_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="GraphOutline" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Loc.RN.Title}"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Margin="0,0,12,0"
                            Background="{DynamicResource SecondaryHueMidBrush}"
                            Click="AIAnalysisFaction_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Brain" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Loc.FM.AIAnalysis}"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- 筛选和搜索 -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <ComboBox x:Name="FactionTypeFilter"
                            materialDesign:HintAssist.Hint="{DynamicResource Loc.FM.TypeHint}"
                            Width="120"
                            Margin="0,0,12,0"
                            SelectionChanged="FactionTypeFilter_SelectionChanged">
                        <ComboBoxItem Tag="全部" Content="{DynamicResource Loc.FM.TypeAll}"/>
                        <ComboBoxItem Tag="宗门" Content="{DynamicResource Loc.FM.Data.宗门}"/>
                        <ComboBoxItem Tag="家族" Content="{DynamicResource Loc.FM.Data.家族}"/>
                        <ComboBoxItem Tag="组织" Content="{DynamicResource Loc.FM.Data.组织}"/>
                        <ComboBoxItem Tag="国家" Content="{DynamicResource Loc.FM.Data.国家}"/>
                        <ComboBoxItem Tag="商会" Content="{DynamicResource Loc.FM.Data.商会}"/>
                    </ComboBox>
                    
                    <ComboBox x:Name="PowerLevelFilter"
                            materialDesign:HintAssist.Hint="{DynamicResource Loc.FM.LevelHint}"
                            Width="120"
                            Margin="0,0,12,0"
                            SelectionChanged="PowerLevelFilter_SelectionChanged">
                        <ComboBoxItem Tag="全部" Content="{DynamicResource Loc.FM.TypeAll}"/>
                        <ComboBoxItem Tag="超级" Content="{DynamicResource Loc.FM.Data.超级}"/>
                        <ComboBoxItem Tag="一流" Content="{DynamicResource Loc.FM.Data.一流}"/>
                        <ComboBoxItem Tag="二流" Content="{DynamicResource Loc.FM.Data.二流}"/>
                        <ComboBoxItem Tag="三流" Content="{DynamicResource Loc.FM.Data.三流}"/>
                        <ComboBoxItem Tag="普通" Content="{DynamicResource Loc.FM.Data.普通}"/>
                    </ComboBox>
                    
                    <TextBox x:Name="SearchTextBox"
                           materialDesign:HintAssist.Hint="{DynamicResource Loc.FM.SearchHint}"
                           materialDesign:TextFieldAssist.HasLeadingIcon="True"
                           materialDesign:TextFieldAssist.LeadingIcon="Search"
                           Width="200"
                           TextChanged="SearchTextBox_TextChanged"/>
                </StackPanel>
            </DockPanel>
        </materialDesign:Card>

        <!-- 主内容区域 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：势力列表 -->
            <materialDesign:Card Grid.Column="0" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 列表标题 -->
                    <Border Grid.Row="0" 
                          Background="{DynamicResource MaterialDesignDivider}" 
                          Padding="16,12">
                        <DockPanel>
                            <TextBlock Text="{DynamicResource Loc.FM.List}" FontWeight="Medium" DockPanel.Dock="Left"/>
                            <ToggleButton x:Name="HierarchyViewToggle"
                                        Style="{StaticResource MaterialDesignActionToggleButton}"
                                        ToolTip="{DynamicResource Loc.FM.HierarchyTooltip}"
                                        DockPanel.Dock="Right"
                                        IsChecked="False"
                                        Checked="ViewMode_Changed"
                                        Unchecked="ViewMode_Changed">
                                <materialDesign:PackIcon Kind="FileTree"/>
                            </ToggleButton>
                        </DockPanel>
                    </Border>

                    <!-- 势力列表 -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="FactionListControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                          BorderThickness="0,0,0,1" 
                                          Padding="16,12"
                                          Background="Transparent"
                                          Cursor="Hand">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" 
                                                        Command="{Binding SelectFactionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                        </Border.InputBindings>
                                        
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- 势力图标 -->
                                            <Border Grid.Column="0" 
                                                  Background="{Binding IconColor}"
                                                  CornerRadius="20"
                                                  Width="40" Height="40"
                                                  Margin="0,0,12,0">
                                                <materialDesign:PackIcon Kind="{Binding IconKind}" 
                                                                       Width="24" Height="24"
                                                                       HorizontalAlignment="Center"
                                                                       VerticalAlignment="Center"
                                                                       Foreground="White"/>
                                            </Border>

                                            <!-- 势力信息 -->
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding DisplayName}" 
                                                         FontWeight="Medium" 
                                                         FontSize="14"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                    <materialDesign:Chip Content="{Binding Type}" 
                                                                       FontSize="10"
                                                                       Height="20"
                                                                       Margin="0,0,4,0"/>
                                                    <materialDesign:Chip Content="{Binding PowerLevel}" 
                                                                       FontSize="10"
                                                                       Height="20"
                                                                       Background="{Binding PowerLevelColor}"/>
                                                </StackPanel>
                                                <TextBlock Text="{Binding MemberCountDisplay}" 
                                                         FontSize="12"
                                                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                         Margin="0,2,0,0"/>
                                            </StackPanel>

                                            <!-- 状态指示 -->
                                            <materialDesign:PackIcon Grid.Column="2" 
                                                                   Kind="ChevronRight"
                                                                   Width="16" Height="16"
                                                                   VerticalAlignment="Center"
                                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </materialDesign:Card>

            <!-- 分隔符 -->
            <GridSplitter Grid.Column="1" 
                          HorizontalAlignment="Stretch" 
                          Background="{DynamicResource MaterialDesignDivider}"/>

            <!-- 右侧：势力详细信息 -->
            <Grid Grid.Column="2" x:Name="DetailArea">
                <!-- 默认显示势力统计信息 -->
                <materialDesign:Card Padding="24" x:Name="DefaultCard">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Loc.FM.Stats}" 
                                 FontSize="24" 
                                 FontWeight="Medium" 
                                 Margin="0,0,0,24"/>

                        <!-- 统计卡片 -->
                        <UniformGrid Columns="2" Margin="0,0,0,24">
                            <materialDesign:Card Margin="0,0,8,8" Padding="16">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource Loc.FM.TotalCount}" 
                                             FontSize="14" 
                                             Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    <TextBlock Text="12" 
                                             FontSize="32" 
                                             FontWeight="Bold"/>
                                </StackPanel>
                            </materialDesign:Card>

                            <materialDesign:Card Margin="8,0,0,8" Padding="16">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource Loc.FM.SectCount}" 
                                             FontSize="14" 
                                             Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    <TextBlock Text="5" 
                                             FontSize="32" 
                                             FontWeight="Bold"/>
                                </StackPanel>
                            </materialDesign:Card>

                            <materialDesign:Card Margin="0,8,8,0" Padding="16">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource Loc.FM.FamilyCount}" 
                                             FontSize="14" 
                                             Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    <TextBlock Text="4" 
                                             FontSize="32" 
                                             FontWeight="Bold"/>
                                </StackPanel>
                            </materialDesign:Card>

                            <materialDesign:Card Margin="8,8,0,0" Padding="16">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource Loc.CM.RelationCount}" 
                                             FontSize="14" 
                                             Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    <TextBlock Text="28" 
                                             FontSize="32" 
                                             FontWeight="Bold"/>
                                </StackPanel>
                            </materialDesign:Card>
                        </UniformGrid>

                        <!-- 实力等级分布 -->
                        <TextBlock Text="{DynamicResource Loc.FM.LevelDistribution}" 
                                 FontSize="18" 
                                 FontWeight="Medium" 
                                 Margin="0,0,0,16"/>

                        <ItemsControl>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <materialDesign:Chip Grid.Column="0" Content="{DynamicResource Loc.FM.LegendSupreme}" Margin="0,0,12,0" Background="#F44336"/>
                                <ProgressBar Grid.Column="1" Value="2" Maximum="12" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="2" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </Grid>

                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <materialDesign:Chip Grid.Column="0" Content="{DynamicResource Loc.FM.LegendFirst}" Margin="0,0,12,0" Background="#FF9800"/>
                                <ProgressBar Grid.Column="1" Value="3" Maximum="12" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="3" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </Grid>

                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <materialDesign:Chip Grid.Column="0" Content="{DynamicResource Loc.FM.LegendSecond}" Margin="0,0,12,0" Background="#4CAF50"/>
                                <ProgressBar Grid.Column="1" Value="4" Maximum="12" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="4" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </Grid>

                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <materialDesign:Chip Grid.Column="0" Content="{DynamicResource Loc.FM.LegendThird}" Margin="0,0,12,0" Background="#2196F3"/>
                                <ProgressBar Grid.Column="1" Value="3" Maximum="12" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="3" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </Grid>
                        </ItemsControl>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
